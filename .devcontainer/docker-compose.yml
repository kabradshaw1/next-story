version: "3.9"
services:
  next:
    build:
      context: ..
      dockerfile: Dockerfile.dev
    volumes:
      - ../:/app:delegated
      - /app/node_modules
    env_file:
      - ../.env.local
    ports:
      - "3000:3000"
      - "3001:3001"
      - "9229:9229"

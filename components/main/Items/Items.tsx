import Image from "next/image";
import Link from "next/link";

type Props = {
  title: string;
  text: string;
};

const Items: React.FC = () => {
  return (
    <div>
      <h1>Items</h1>
    </div>
  );
};

export default Items;
